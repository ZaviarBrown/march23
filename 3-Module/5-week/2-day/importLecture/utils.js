export const rng = (num) => Math.floor(Math.random() * num);
export const strSplitter = (str) => str.split(" ");

export default () => console.log("DEFAULT");

// const rng = (num) => Math.floor(Math.random() * num);
// export default rng;

// export default (num) => Math.floor(Math.random() * num);
//? --------------------------------------------------------------------
//? ---------------------------------  ---------------------------------
//? --------------------------------------------------------------------

// const rng = (num) => Math.floor(Math.random() * num);
// const strSplitter = (str) => str.split(" ");

// module.exports = rng;

// // module.exports = {
// //     rng,
// //     strSplitter,
// // };
// // // rng: rng,
