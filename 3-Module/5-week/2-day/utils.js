// const rng = (num) => Math.floor(Math.random() * num);

// const strSlicer = (str) => str.split(" ");

// module.exports = rng;

// // module.exports = { rng };

// // module.exports = { rng, strSlicer };c

//? --------------------------------------------------------------------
//? ---------------------------------  ---------------------------------
//? --------------------------------------------------------------------
const rng = (num) => Math.floor(Math.random() * num);

export default rng;

export const strSlicer = (str) => str.split(" ");
